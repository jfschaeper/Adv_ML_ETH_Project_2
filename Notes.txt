The time between R peaks should always be the same
baseline should not be elavated
60 to 100 BPS
one P wave for every QRS
QRS should be short
PR time not too long
distinct p and t wave

Code:
which point does the peak index indicate, the beginning, the highest point? (onset_qrs, end_qrs)

r_peaks[i][r_peaks[i]>0]?

rr_dist = rr_dist[rr_dist < 300] # get rid of heart beats far removed from one another (this seems actually super important and this is just one sec here)

if len(r_i) == len(q_i) == len(s_i): (the cases without this regularity are actually very important)

convert list append to Dataframe entrymaking

# around 300 rows (5%) of this matrix contain missing values for the variables related to length of the qrs sector 
# and the amplitude.


Links:
https://www.kaggle.com/gregoiredc/arrhythmia-on-ecg-classification-using-cnn/notebook
https://www.kaggle.com/shayanfazeli/heartbeat/code?datasetId=29414&sortBy=voteCount
